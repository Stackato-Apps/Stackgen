name: stackgen

framework:
  type: node

mem: 128M

services:
  ${name}-mongo: mongodb
  ${name}-yamls: filesystem

hooks:
  pre-running:
    - mv public/yamls public/yamls-bkk || true
    - ln -s $STACKATO_FILESYSTEM public/yamls
